describe('foodIdeas.searchRestaurant', function() {

  /*var SearchRestaurant;

  beforeEach(module('foodIdeas'));
  beforeEach(inject(function($injector) {
    SearchRestaurant = $injector.get('searchRestaurant');
  }));*/

  it('First test', function() {
    expect(true).toEqual(true);
  });

});
